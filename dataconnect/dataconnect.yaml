specVersion: "v1beta"
serviceId: "angular-join"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "angular-join-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
